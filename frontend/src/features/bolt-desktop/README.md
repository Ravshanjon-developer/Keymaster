# Bolt Desktop Simulator

Windows-style file explorer used by `/simulator?mode=desktop` and course **Первый ноутбук**.

Completing a mission credits the matching lesson (`keys: desktop:N`) through `/training/submit`.
